---
const currentPath = Astro.url.pathname;

const links = [
  { href: '/', label: 'Home' },
  { href: '/projects', label: 'Projects' },
  { href: '/blog', label: 'Blog' },
  { href: '/contact', label: 'Contact' },
];
---

<nav class="w-full border-b border-border">
  <div class="max-w-content mx-auto px-6 py-5 flex items-center justify-between">
    <a href="/" class="font-serif text-lg tracking-heading no-underline text-ink hover:text-accent transition-colors">
      AHP
    </a>
    <ul class="flex gap-6 list-none m-0 p-0">
      {links.map((link) => (
        <li>
          <a
            href={link.href}
            class:list={[
              'text-sm no-underline transition-colors',
              currentPath === link.href
                ? 'text-accent font-medium'
                : 'text-ink-muted hover:text-ink',
            ]}
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>
  </div>
</nav>
